# Anthropic API key — get yours at console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# Trustpilot session cookie — optional, but unlocks full review scraping
# Without this, the tool is capped at ~600 reviews (anonymous multi-sort)
# See README for how to extract this from your browser
TRUSTPILOT_COOKIE=
